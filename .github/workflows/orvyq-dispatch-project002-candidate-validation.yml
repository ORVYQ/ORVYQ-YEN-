name: Dispatch Project 002 Candidate Validation

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/orvyq-dispatch-project002-candidate-validation.yml"

permissions:
  actions: write
  contents: read
  issues: write
  pull-requests: write

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch candidate validation on Project 002 branch
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          gh workflow run orvyq-candidate-validation.yml \
            --repo "${{ github.repository }}" \
            --ref agent/002-deep-sea-cold-war \
            -f project_id=002-the-new-war-beneath-the-ocean

      - name: Report dispatch to Project 002 PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh api repos/${{ github.repository }}/issues/3/comments \
            -f body="Project 002 Candidate Validation re-dispatched on branch head \`514aa06\` after testing additional real capacity in the CLM_014 authored cards. The 8% generic-card quality ceiling remains unchanged."

# Trigger revision: 2026-07-28T21:15+03:00; Project 002 head 514aa06d71306d80611f94f497a148d7d8bec9c2.
