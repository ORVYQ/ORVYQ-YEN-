name: ORVYQ Final Encode

# Manual, approval-gated, encode-only workflow. It consumes the exact immutable
# render bundle produced for one explicitly selected project and never rebuilds
# creative data.
on:
  workflow_dispatch:
    inputs:
      project_id:
        description: "Project directory id under projects/"
        type: string
        required: true
      quality_control_approved:
        description: "Human approved the full-length review referenced by the project's approval record"
        type: boolean
        default: false
      approved_review_run_id:
        description: "Review workflow run ID that produced the approved render bundle"
        type: string
        required: true

permissions:
  contents: read
  actions: read

concurrency:
  group: orvyq-final-encode-${{ inputs.project_id }}
  cancel-in-progress: false

env:
  PROJECT_ID: ${{ inputs.project_id }}
  FINAL_WIDTH: "1920"
  FINAL_HEIGHT: "1080"
  FINAL_CRF: "16"

jobs:
  final-encode:
    if: ${{ inputs.quality_control_approved == true && inputs.approved_review_run_id != '' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact source
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate selected project production profile
        run: |
          set -euo pipefail
          node --input-type=module -e "
            import { loadProductionPolicy } from './scripts/lib/orvyq-project-profile.mjs';
            await loadProductionPolicy(process.env.PROJECT_ID);
          "

      # The mirror of orvyq-review.yml's "Verify the named Candidate Validation
      # run" check. Without it the approval record was the only thing tying
      # this encode to a review run, so a run id naming a different workflow,
      # a different commit, or a review that FAILED its post-render QA was
      # accepted as long as the committed approval quoted the same number.
      - name: Verify the named Full-Length Review run
        uses: actions/github-script@v7
        with:
          script: |
            const runId = Number('${{ inputs.approved_review_run_id }}');
            const { data: run } = await github.rest.actions.getWorkflowRun({ owner: context.repo.owner, repo: context.repo.repo, run_id: runId });
            const failures = [];
            if (!run.path || !run.path.endsWith("orvyq-review.yml")) failures.push(`run ${runId} is not a Full-Length Candidate Review`);
            if (run.conclusion !== "success") failures.push(`run ${runId} conclusion is ${run.conclusion}`);
            if (run.head_sha !== context.sha) failures.push(`run ${runId} rendered ${run.head_sha}, expected ${context.sha}`);
            if (failures.length) core.setFailed(failures.join("; "));

      - name: Verify approval record exists and names this review run
        run: |
          set -euo pipefail
          test -s "projects/$PROJECT_ID/qa/proof_approval.json"
          node -e "
            const approval = require('./projects/$PROJECT_ID/qa/proof_approval.json');
            if (approval.approved !== true) throw new Error('approval.approved is not true');
            if (String(approval.review_run_id) !== String('${{ inputs.approved_review_run_id }}')) throw new Error('approval review_run_id does not match workflow input');
          "

      - name: Download approved project render bundle
        uses: actions/download-artifact@v4
        with:
          name: orvyq-render-bundle-${{ inputs.project_id }}-${{ inputs.approved_review_run_id }}
          path: render-bundle
          run-id: ${{ inputs.approved_review_run_id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify bundle project identity
        run: |
          set -euo pipefail
          test "$(cat render-bundle/render-source-sha.txt)" = "$GITHUB_SHA"
          node -e "
            const profile = require('./render-bundle/projects/$PROJECT_ID/config/production_profile.json');
            const manifest = require('./render-bundle/projects/$PROJECT_ID/qa/review_render_manifest.json');
            if (profile.project_id !== process.env.PROJECT_ID) throw new Error('bundle production profile project mismatch');
            if (manifest.project_id && manifest.project_id !== process.env.PROJECT_ID) throw new Error('review manifest project mismatch');
          "

      # Placed exactly the way orvyq-review.yml places the validated bundle:
      # the whole assets tree comes from the approved bundle, not a hand-listed
      # subset. Copying only six named files left every other frozen asset
      # (footage, evidence images, sfx) coming from whatever the checkout
      # happened to contain, which is not what section 9 of ORVYQ_SYSTEM.md
      # means by "the exact approved canonical timeline".
      - name: Place downloaded immutable bundle into selected project tree
        run: |
          set -euo pipefail
          SRC="render-bundle/projects/$PROJECT_ID"
          DEST="projects/$PROJECT_ID"
          test -d "$SRC/assets"
          test -d "$SRC/remotion/render_ready_project"
          mkdir -p "$DEST/config" "$DEST/direction" "$DEST/remotion" "$DEST/qa"
          cp "$SRC/config/production_profile.json" "$DEST/config/production_profile.json"
          cp "$SRC/direction/edit_plan.json" "$DEST/direction/edit_plan.json"
          cp "$SRC/remotion/captions.json" "$DEST/remotion/captions.json"
          cp "$SRC/qa/frozen_candidate.json" "$DEST/qa/frozen_candidate.json"
          cp "$SRC/qa/review_render_manifest.json" review_render_manifest.json
          rm -rf "$DEST/assets"
          cp -r "$SRC/assets" "$DEST/assets"
          rm -rf "$DEST/remotion/render_ready_project"
          cp -r "$SRC/remotion/render_ready_project" "$DEST/remotion/render_ready_project"

      - name: Verify approval matches downloaded candidate
        run: node scripts/orvyq_verify_approval.mjs --project-id="$PROJECT_ID" --mode=full --stage=early --approved-review-run-id="${{ inputs.approved_review_run_id }}"

      # The same check orvyq-review.yml runs, replacing a hand-inlined
      # six-file hash comparison that silently skipped the asset manifest
      # (every binary asset) and the render_ready_project tree hash -- the
      # deliverable encode was verifying strictly less than the review did.
      - name: Verify every downloaded source and binary asset
        run: node scripts/orvyq_verify_bundle_integrity.mjs --project-id="$PROJECT_ID"

      - name: Validate repository-local footage and provenance
        run: node scripts/orvyq_materialize_footage.mjs --project-id="$PROJECT_ID"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # faster-whisper is required, not optional: orvyq_media_qa.mjs reads
      # qa/speech_transcript.json, which is gitignored build output and is not
      # carried in the render bundle, so it has to be produced against this
      # run's own rendered file exactly as orvyq-review.yml does.
      - name: Install media and speech tooling
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends ffmpeg
          python -m pip install --disable-pip-version-check faster-whisper==1.2.0 requests==2.32.5

      - name: Verify placed candidate still matches approval
        run: node scripts/orvyq_verify_approval.mjs --project-id="$PROJECT_ID" --stage=late

      - name: Install Remotion dependencies and type-check
        working-directory: projects/${{ env.PROJECT_ID }}/remotion/render_ready_project
        run: |
          npm ci --no-audit --no-fund
          npx tsc --noEmit

      - name: Resolve browser executable
        run: |
          set -euo pipefail
          BROWSER="$(command -v google-chrome-stable || command -v google-chrome || command -v chromium-browser || command -v chromium || true)"
          test -n "$BROWSER"
          echo "FACTFORGE_REMOTION_BROWSER_EXECUTABLE=$BROWSER" >> "$GITHUB_ENV"

      - name: Render full candidate at final quality
        working-directory: projects/${{ env.PROJECT_ID }}/remotion/render_ready_project
        run: npx remotion render src/index.ts FactForgeVideo out/final_video.mp4 --codec=h264 --crf="$FINAL_CRF"

      - name: Compute final video sha256
        run: |
          set -euo pipefail
          echo "FINAL_VIDEO_SHA256=$(sha256sum "projects/$PROJECT_ID/remotion/render_ready_project/out/final_video.mp4" | cut -d' ' -f1)" >> "$GITHUB_ENV"

      # Same shared builder orvyq-review.yml uses, so the two manifests this
      # workflow's parity check compares are guaranteed to share one shape and
      # one schema version (see scripts/orvyq_render_manifest.mjs).
      - name: Write final render manifest
        run: |
          set -euo pipefail
          node scripts/orvyq_render_manifest.mjs \
            --project-id="$PROJECT_ID" \
            --profile=final \
            --output="projects/$PROJECT_ID/qa/final_render_manifest.json" \
            --run-id="${{ github.run_id }}" \
            --video-path="out/final_video.mp4" \
            --video-sha256="$FINAL_VIDEO_SHA256" \
            --width="$FINAL_WIDTH" \
            --height="$FINAL_HEIGHT" \
            --codec=h264 \
            --crf="$FINAL_CRF"

      - name: Verify review and final parity
        run: node scripts/orvyq_review_final_parity.mjs --review-manifest review_render_manifest.json --final-manifest "projects/$PROJECT_ID/qa/final_render_manifest.json" --report "projects/$PROJECT_ID/qa/review_final_parity.json"

      - name: Verify speech in the final render
        run: python scripts/orvyq_speech_qa.py --project-id "$PROJECT_ID" --media "projects/$PROJECT_ID/remotion/render_ready_project/out/final_video.mp4" --output-name speech_transcript.json

      - name: Run post-render media QA
        run: |
          set +e
          node scripts/orvyq_media_qa.mjs --project-id "$PROJECT_ID" --video "projects/$PROJECT_ID/remotion/render_ready_project/out/final_video.mp4" --report "projects/$PROJECT_ID/qa/orvyq_full_media_qa.json" > media-qa.log 2>&1
          echo $? > media-qa.status
          cat media-qa.log
          exit 0

      - name: Copy final output
        run: |
          set -euo pipefail
          mkdir -p "projects/$PROJECT_ID/output"
          cp "projects/$PROJECT_ID/remotion/render_ready_project/out/final_video.mp4" "projects/$PROJECT_ID/output/final_video.mp4"
          {
            echo "Project: $PROJECT_ID"
            echo "Rendered at: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo "Workflow run: ${{ github.run_id }}"
            echo "Approved review run: ${{ inputs.approved_review_run_id }}"
            echo "Source commit: ${{ github.sha }}"
          } > "projects/$PROJECT_ID/output/render_log.txt"

      - name: Upload final render
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: orvyq-final-${{ inputs.project_id }}-${{ github.run_id }}
          retention-days: 30
          path: |
            projects/${{ env.PROJECT_ID }}/output/final_video.mp4
            projects/${{ env.PROJECT_ID }}/output/render_log.txt
          if-no-files-found: error

      - name: Upload final QA reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: orvyq-final-qa-${{ inputs.project_id }}-${{ github.run_id }}
          retention-days: 30
          path: |
            projects/${{ env.PROJECT_ID }}/qa/*.json
            media-qa.log
            media-qa.status

      - name: Stop when post-render media QA failed
        run: test "$(cat media-qa.status)" = "0"
